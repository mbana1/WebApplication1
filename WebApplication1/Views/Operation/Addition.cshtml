
@model WebApplication1.Models.Operation
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Addition</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css">
   
</head>
<body>
    <div>
        
       <table class="table table-sm table-bordered table-dark">
            <thead>

                <tr>
                    <th  scope="col" class="text-center">Operande gauche</th>
                    <th  scope="col"class="text-center">Opérateur</th>
                    <th  scope="col" class="text-center">Operande droite</th>
                    <th  scope="col" class="text-center"></th>
                    <th scope="col" class="text-center">Résultat :</th>
                </tr>
                <tr class="table-primary" >

                    <td id="z" class="text-center" scope="row">@Model.OperandeGauche  </td>
                    <td class="text-center">@Model.Operateur</td>
                    <td class="text-center">@Model.OperandeDroite</td>
                    <td class="text-center">=</td>
                    <td class="text-center">@Model.GetResultat()</td>
                </tr>

            </thead>
        </table>
    </div>
</body>
</html>
